# Módulo Python

Protótipo da camada de aplicação do projeto.

## Execução

Na pasta `python/`:

```bash
python app.py
```

Para interface web:

```bash
pip install -r requirements.txt
streamlit run app.py
```

A base utilizada no protótipo é SQLite, adequada para desenvolvimento e demonstração local.
